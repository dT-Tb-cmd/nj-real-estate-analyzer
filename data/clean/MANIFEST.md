# Clean Data Manifest

Every table written to `data/clean/housing.db` gets a row here.
The clean database is never committed — this manifest tells you what
tables exist and where they came from.

Populated during Step 3 (data cleaning), not Step 2.

## Tables

| Table | Source files (from raw) | Rows | Loaded | Notes |
|---|---|---|---|---|
| *(none yet — filled during Step 3)* | | | | |
